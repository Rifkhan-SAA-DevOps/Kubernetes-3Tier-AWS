# k8s/backend/backend-configmap.yaml

apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: library-app

data:
  DB_HOST: mysql-service
  DB_NAME: library_db
  DB_PORT: "3306"

  PORT: "5000"
  DB_USER: appuser
  JWT_EXPIRES_IN: "1d"