apiVersion: v1
kind: Service
metadata:
  name: backend-nodeport
  namespace: three-tier-app

spec:
  type: NodePort

  selector:
    app: backend

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 8080
      nodePort: 30080